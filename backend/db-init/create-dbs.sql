-- Create database for application
CREATE DATABASE IF NOT EXISTS spk_profile_matching CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Create database for testing
CREATE DATABASE IF NOT EXISTS spk_profile_matching_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust as needed)
-- GRANT ALL PRIVILEGES ON spk_profile_matching.* TO 'root'@'%';
-- GRANT ALL PRIVILEGES ON spk_profile_matching_test.* TO 'root'@'%';
-- FLUSH PRIVILEGES;
